import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      <nav>
        <h1>United Vitamins of India</h1>
        {/* Add navigation links if any */}
      </nav>
    </header>
  );
};

export default Header;
